<h1>All the movies !!</h1>
<ul>
    
{{#each movies}}
    <div>
        <img src="{{this.image}}" alt="image">
        <h2>{{this.title}}</h2>
        <a href="/movies/{{_id}}">See More</a>
    </div>
{{/each }}
</ul>